import React from 'react';

export const Admin = () => (
  <div className="container">
    <h1>Admin Panel</h1>
    <p>Here, admins can add custom foods or manage users (to be implemented).</p>
  </div>
);
